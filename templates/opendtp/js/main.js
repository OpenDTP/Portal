jQuery( document ).ready(function( $ ) {
    // prepare for team content
    
    // Carousel
    $('myCarousel').carousel({
        interval: 7000,
        pause: 'hover',
        wrap: true
    });
});